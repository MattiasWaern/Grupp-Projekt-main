{
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  },
  "dependencies": {
    "sass-embedded": "^1.80.1"
  },

  "scripts": {
    "watch:sass": "sass style.scss style.css",
    "watch:tailwind": "npx tailwindcss -i style.css -o style.css --minify --watch",
    "build": "npm run watch:sass && npm run watch:tailwind"
  }

}
